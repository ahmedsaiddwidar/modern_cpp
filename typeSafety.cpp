//ability of the language to prevent type errors during compilation or runtime by enforcing strict rules on type conversions,\
assignments, and operations.

#include<iostream>
int main(){
    int a = 5;
    decltype(a) b = 10; 
}
